# 暗記アプリ 仕様書 v3

> v2からの主な変更：組み合わせ・順番当てへの部分点（3値判定）導入、スコア算出方法の定義、ReviewStateへのpartialCount追加、関連する設定項目・CSV列・リザルト表示の更新。

## 0. 概要

| 項目 | 内容 |
|---|---|
| アプリ種別 | PWA（インストール可能・オフライン動作） |
| 配信 | GitHub Pages（静的ホスティング） |
| バックエンド | なし |
| データ保存 | IndexedDB |
| 対象ブラウザ | Google Chrome / Safari（最新版） |
| 対象デバイス | スマートフォン、タブレット、PC |
| 開発環境 | Claude Code |

---

## 1. データモデル

### 1.1 Deck（デッキ）

| フィールド | 型 | 説明 |
|---|---|---|
| deckId | string(UUID) | デッキID |
| name | string | デッキ名（編集可） |
| createdAt | string(ISO8601) | 作成日時 |
| updatedAt | string(ISO8601) | 更新日時 |

設定はすべてグローバル設定（1.5）に集約する。

### 1.2 Card（カード）

| フィールド | 型 | 説明 |
|---|---|---|
| cardId | string(UUID) | カードID |
| deckId | string(UUID) or null | 所属デッキID。nullは「デッキ未所属カード」（4.8参照） |
| type | enum | `single` / `choice` / `matching` / `ordering` / `anagram` |
| question | string | 問題文 |
| choices | string[] (最大6) | 形式に応じた選択肢データ（2.参照） |
| answerIndex | number or null | **多肢選択(choice)専用**。正解のchoices配列インデックス（0起点） |
| answer | string | 解答文字列。`single` / `anagram` で使用 |
| explanation | string | 解説文 |
| createdAt | string(ISO8601) | 作成日時 |
| updatedAt | string(ISO8601) | 更新日時 |

**インデックス**：`deckId`

### 1.3 ReviewState（回答状況）

カードごとに1件。履歴の全件保持はしない。

| フィールド | 型 | 説明 |
|---|---|---|
| cardId | string(UUID) | 対象カードID（主キー） |
| deckId | string(UUID) or null | 抽出高速化用。Card.deckIdと常に同期 |
| repetition | number | 連続正解回数（初期値0） |
| interval | number | 現在の間隔（日数、初期値0） |
| easiness | number | 易しさ係数EF（初期値は設定値、既定2.5） |
| dueDate | string(YYYY-MM-DD) | 次回出題予定日。新規カードは登録日（＝即出題対象） |
| lastResult | enum | `correct` / **`partial`** / `incorrect` / `unanswered`（初期値 `unanswered`） |
| lastScore | number or null | **直近の一致率（0.0〜1.0）**。組み合わせ・順番当てのみ記録。他形式はnull |
| lastAnsweredAt | string(ISO8601) or null | 直近回答日時 |
| totalCount | number | 累計回答回数 |
| correctCount | number | 累計完答回数 |
| **partialCount** | number | **累計部分正解回数** |
| checked | boolean | チェックフラグ |
| suspendedUntil | string(YYYY-MM-DD) or null | スキップによる一時除外期限（5.4参照） |

**インデックス**：`[deckId, dueDate]`（複合）、`[deckId, checked]`

### 1.4 SetItem（セット実施中データ・非永続）

| フィールド | 型 | 説明 |
|---|---|---|
| cardId | string | 対象カード |
| result | enum | `correct` / `partial` / `incorrect` / `skipped` / `pending` |
| score | number or null | 一致率（組み合わせ・順番当てのみ） |
| nextDueDate | string or null | このセットで算出された次回出題日 |
| prevReviewState | ReviewState or null | 回答直前のReviewStateの完全なコピー（やり直し用） |

### 1.5 GlobalSettings（グローバル設定）

| フィールド | 既定値 | 説明 |
|---|---|---|
| setSize | 20 | セットの問題数（1〜100）。nullは「設定なし＝対象カード全件」 |
| scope | `review` | `review`（復習） / `all`（全て） |
| shuffleAll | true | 「全て」モードで出題順をランダム化するか |
| firstInterval | 1 | repetition=1時のinterval（日） |
| secondInterval | 6 | repetition=2時のinterval（日） |
| intervalFactor | 1.0 | 算出intervalに掛ける倍率（0.1〜5.0） |
| efInitial | 2.5 | 新規カードのEF初期値 |
| efMin | 1.3 | EF下限 |
| efGainOnCorrect | 0.1 | 正解（完答）時のEF加算値 |
| efLossOnIncorrect | 0.2 | 不正解時のEF減算値 |
| **partialThreshold** | **0.5** | **部分正解と判定する一致率の下限（0.0〜1.0）** |
| **partialIntervalFactor** | **1.0** | **部分正解時のinterval倍率。1.0で据え置き、1.2等で緩やかに伸長** |
| **orderingScoreMethod** | **`concordance`** | **順番当てのスコア算出方式（3.3参照）** |
| lastExportedAt | null | 最終エクスポート日時（バックアップ警告用） |

---

## 2. 問題形式ごとのデータ構造

| 形式 | choices | answerIndex | answer | 判定 |
|---|---|---|---|---|
| 1問1答 (single) | 空 | null | 解答文字列 | 2値（自己申告） |
| 多肢選択 (choice) | 最大6件 | 正解のインデックス(0起点) | 任意 | 2値 |
| 組み合わせ (matching) | `左:右` 最大6件 | null | 未使用 | **3値（部分点あり）** |
| 順番当て (ordering) | 最大6件（登録順＝正解順） | null | 未使用 | **3値（部分点あり）** |
| 並べ替え (anagram) | 空 または カンマ区切り1件 | null | 完成形の文字列 | 2値（自己申告） |

### 2.1 多肢選択の正解判定

`answerIndex` によるインデックス一致で判定する（文字列一致は使わない）。

### 2.2 組み合わせ（matching）のエスケープ規則

各choice要素は `左:右` 形式。区切り文字を値に含める場合は以下のエスケープを適用する。

| 記述 | 意味 |
|---|---|
| `\:` | 文字としてのコロン `:` |
| `\\` | 文字としてのバックスラッシュ `\` |

**パース規則**：先頭から1文字ずつ走査し、`\` の次の1文字はエスケープ済みとして区切り判定から除外する。**エスケープされていない最初のコロンのみを区切りとする**（2つ目以降のコロンは右側の値の一部）。

例：
- `開始時刻\:12\:30:Start Time` → 左＝`開始時刻:12:30` / 右＝`Start Time`
- `C\\Users:パス` → 左＝`C\Users` / 右＝`パス`

**エンコード規則**：左右の値それぞれについて `\` → `\\` に置換したうえで `:` → `\:` に置換し、`左:右` として連結する（置換順序はバックスラッシュが先）。

**バリデーション**：エスケープされていないコロンが存在しない要素は不正としてエラーにする。

### 2.3 並べ替え（anagram）の表示ロジック

- **choicesが空**：answer文字列を1文字ずつ分解してランダム表示（例：解答「富士山」→「山 富 士」）
- **choicesにカンマ区切り文字列が1件**：カンマで分割した単位をランダム表示（例：`ふ,じ,さ,ん` →「さ じ ふ ん」）。「ファ」等を1トークンとして扱う用途

ユーザーは並べ替え操作を行わず、完成形を想起して○/×を自己判定する。

### 2.4 シャッフル規則

`choice` / `matching` / `ordering` / `anagram` のすべてで、表示時に要素順をランダムにシャッフルする。

- シャッフル結果が元の登録順と完全一致した場合は**再シャッフル**する
- 要素数1の場合はシャッフル不要

---

## 3. 正誤判定とSM-2ロジック

### 3.1 判定値の3値化

| 形式 | 判定 |
|---|---|
| 1問1答 / 並べ替え | ユーザーの ○（correct）/ ×（incorrect）自己申告 |
| 多肢選択 | 正解（correct）/ 不正解（incorrect） |
| **組み合わせ / 順番当て** | **一致率 score を算出し、下記により3値判定** |

```
score == 1.0                      → correct   （完答）
partialThreshold <= score < 1.0   → partial   （部分正解：評価を下げない）
score < partialThreshold          → incorrect （不正解）
```

`partialThreshold` の既定値は0.5（半分以上一致で部分正解）。

### 3.2 組み合わせ（matching）のスコア算出

```
score = 正しく組み合わせられたペア数 / 全ペア数
```

例：6ペア中4ペア正解 → score = 0.667 → 部分正解

### 3.3 順番当て（ordering）のスコア算出

既定は **相対順序一致率（concordance）** を用いる。

```
score = 正しい相対順序にある要素ペア数 / nC2
```

全要素の組み合わせ（nC2通り）について、2要素の前後関係が正解と一致するかを数える。

**この方式を既定とする理由**：単純な「位置一致率」では、要素が1つずれるだけで全要素の位置が不一致となり、実質的にほぼ正解の解答でもスコア0になる。例えば正解 `ABCDEF` に対し解答 `BCDEFA` は、位置一致率では0だが、相対順序一致率では10/15＝0.667となり実態に近い評価になる。

**代替方式**：`orderingScoreMethod` に `position`（位置一致率＝ 正しい位置にある要素数 / 全要素数）を設定することで切り替え可能とする。厳密な順序記憶を求める場合に使用する。

### 3.4 SM-2 更新式

**correct（正解・完答）**
```
repetition = repetition + 1
if repetition == 1: base = firstInterval
elif repetition == 2: base = secondInterval
else:                 base = interval * easiness

interval  = max(1, round(base * intervalFactor))
easiness  = easiness + efGainOnCorrect
dueDate   = 回答日 + interval 日

totalCount += 1
correctCount += 1
```

**partial（部分正解・評価を下げない）**
```
repetition : 変更しない
easiness   : 変更しない

base     = (interval > 0) ? interval : max(1, round(firstInterval * intervalFactor))
interval = max(1, round(base * partialIntervalFactor))
dueDate  = 回答日 + interval 日

totalCount += 1
partialCount += 1
```

`partialIntervalFactor = 1.0`（既定）の場合、間隔は現状維持となり、伸びも縮みもしない。1.0より大きい値を設定すると、部分正解を繰り返すことで緩やかに間隔が伸びる。

**incorrect（不正解）**
```
repetition = 0
interval   = max(1, round(firstInterval * intervalFactor))
easiness   = max(easiness - efLossOnIncorrect, efMin)
dueDate    = 回答日 + interval 日

totalCount += 1
```

**共通後処理**
```
lastResult     = correct / partial / incorrect
lastScore      = score（組み合わせ・順番当てのみ。他形式はnull）
lastAnsweredAt = 現在日時
```

EFに上限は設けない（下限のみ `efMin`）。

---

## 4. 画面仕様

### 4.1 ホーム／デッキ一覧画面

- デッキ一覧（デッキ名、カード枚数、本日の要復習数）
- デッキ選択 → カード一覧画面
- 「暗記開始」→ カード画面（グローバル設定に従って出題）
- 新規デッキ作成（空デッキ作成 / CSVインポート）への導線
- 「デッキ未所属カード」への導線（4.8）
- **PWAインストール案内**：未インストール時、画面上部に「ホーム画面に追加してください」の説明を常時表示（6.2）
- **バックアップ警告**：`lastExportedAt` から既定14日以上経過、または未エクスポートの場合に警告表示

### 4.2 デッキ管理

- **デッキ作成**：デッキ名を入力して空デッキを作成
- **デッキ名編集**：既存デッキの名称変更
- **デッキ削除**：削除時に「このデッキのカードも削除しますか？」を確認
  - 「カードも削除」→ Card / ReviewState を物理削除
  - 「カードを残す」→ Card.deckId と ReviewState.deckId を `null` に更新し、デッキのみ削除

### 4.3 CSVインポート画面

- ファイル選択 → フォーマットバリデーション（7.3）。エラー時は行番号とエラー内容を一覧表示し、**インポート全体を中止**（部分適用しない）
- インポート先：新規デッキ（デッキ名入力）または既存デッキを選択
- **既存IDが含まれる場合の更新オプション**（IDが既存カードと一致する行にのみ適用）
  - ☐ 問題データを更新する（type / question / choices / answerIndex / answer / explanation）
  - ☐ 回答状況を取り込む（repetition / interval / easiness / dueDate / lastResult / lastScore / lastAnsweredAt / totalCount / correctCount / partialCount / checked / suspendedUntil）
  - 両方OFFの場合はスキップ

### 4.4 カード一覧画面

- 一覧表示（問題文冒頭、形式、チェック有無、次回出題日）
- **検索**：問題文・選択肢・解答・解説を対象としたインクリメンタル検索
- **フィルタ**：チェック付きのみ表示
- カードタップ → カード編集画面
- **カード新規作成ボタン** → カード作成画面（4.5）
- **エクスポート**：CSV出力。**絞り込み中の場合は表示中のカードのみ**、絞り込みなしの場合は全件。実行時に `lastExportedAt` を更新

### 4.5 カード作成／編集画面

- 新規作成時：cardIdをUUIDで発行し、ReviewStateを初期値で同時作成（`dueDate` ＝当日、`lastResult` ＝ `unanswered`）
- 編集項目：type / question / choices（最大6、動的追加・削除・並べ替え）/ answerIndex / answer / explanation / checked
- type変更時は入力UIを切り替え、使用しなくなるフィールドがある場合は警告表示
- **カード削除**機能（Card / ReviewState を物理削除）
- 保存時に `updatedAt` を更新

### 4.6 設定画面

「表示」と「出題と採点」の2区分で構成する。「表示」は表示モード（6.5）とテーマ（6.4）、
「出題と採点」は 1.5 GlobalSettings の全項目を編集する。

- 表示モード（おとな / こども。6.5）
- テーマ（端末の設定に従う / ライト / ダーク）
- セットの問題数（1〜100 / 設定なし）
- 出題範囲（復習 / 全て）、「全て」時の順序ランダム化
- SM-2パラメータ（firstInterval, secondInterval, intervalFactor, efInitial, efMin, efGainOnCorrect, efLossOnIncorrect）
- **部分点パラメータ**（partialThreshold, partialIntervalFactor, orderingScoreMethod）
- 各項目に「既定値に戻す」を用意

### 4.7 カード画面（セット実施中）

- 問題文と、形式に応じた回答UIを表示
  - **選択肢あり（choice / matching / ordering）**：選択完了時に自動で判定し、解答・解説を表示してから「次へ」で進む
  - **選択肢なし（single / anagram）**：画面下部の操作バーに「解答を表示」（左）と「わかる」（右）を置く
    - **解答を表示**：その画面に解答と解説を表示し、操作を「あとでやる」（左＝不正解）／「おぼえた」（右＝正解）に入れ替える。
      どちらかを選ぶと回答を確定して次の問題へ進む
    - **わかる**：正解として確定し、そのまま次の問題へ進む
    - 次の問題では操作バーの上部に**前問の解答のみ**を表示する（誤りに気付いた場合は「やり直し」で戻る）。
      その問題の解答を表示（または判定）した時点で、前問の解答は消す
    - 最終問題では次へ進まず、その画面に解答・解説を表示して「リザルトへ」を表示する
- 判定後に解答・解説を表示する。**「解答」「解説」の見出しは置かず**、文字の大きさとカードの配色で区別する
  （解答＝大きく濃い文字の色付きカード、解説＝小さく控えめな文字の淡いカード）
- **判定区分（完答 / 部分正解 / 不正解）の文字は表示しない。** 次回出題日の右に評価の推移を矢印で示す
  - `↗` 上がった（完答）／ `→` 変わらず（部分正解・スキップ）／ `↘` 下がった（不正解）。矢印は判定区分の色を用いる
- **組み合わせ・順番当ての判定結果表示**
  - **スコア（例：4/6 正解、順序一致率 67%）**を表示
  - どの要素が誤っていたかを視覚的に示す（誤ったペア・位置をハイライト）
- 進捗表示（例：3 / 20）

**画面内機能：**

| 機能 | 挙動 |
|---|---|
| **やり直し** | 1問前に戻る。セット先頭まで繰り返し遡れる。対象問題の `prevReviewState` をReviewStateへ書き戻して回答を取り消し、SetItemの `result` を `pending` に戻す |
| **スキップ** | 現在の問題を回答せず次へ進む。回答済みの場合は先にReviewStateを復元してから実行。挙動は出題範囲により異なる（5.4） |
| **中断する** | セットを終了し、その時点までの回答でリザルト画面（4.9）を表示 |
| **チェック** | ReviewState.checked をトグル（即時保存） |

### 4.8 デッキ未所属カード画面

- `deckId` が null のカードを一覧表示
- カード一覧画面と同等の検索・フィルタ・エクスポート機能
- カード編集・削除、既存デッキへの移動が可能
- 未所属カードは暗記セットの出題対象外

### 4.9 リザルト画面

- セット内で回答／スキップした問題の一覧
  - 問題文（冒頭）
  - 次回出題日と**評価の推移を表す矢印**（4.7と同じ `↗` / `→` / `↘`。判定区分の文字は表示しない）
  - 部分点のある形式はスコア（%）を併記。スキップした行のみ「スキップ」と表示する
- 各行から選択肢・解説を展開表示できる
- 各行でチェックフラグをその場でON/OFFできる（即時保存）
- セット全体のサマリ（矢印ごとの件数 / スキップ数）を表示
- **続ける**：同じデッキで新しいセットを組み直す（デッキ一覧の「暗記開始」と同じ挙動）

---

## 5. 主要フロー・ルール

### 5.1 出題対象の抽出

**出題範囲＝復習（review）**

1. 対象デッキのカードのうち、以下を満たすものを抽出
   - `dueDate <= 今日`
   - かつ `suspendedUntil` が null または `<= 今日`
2. 並び順：**復習カード（`lastResult != unanswered`）を優先**し `dueDate` 昇順。続いて新規カード（`lastResult == unanswered`）を `createdAt` 昇順で連結
3. `setSize` 件で切り出す（null の場合は全件）

**出題範囲＝全て（all）**

1. 対象デッキの全カードを対象（`dueDate` / `suspendedUntil` は考慮しない）
2. 並び順：`shuffleAll = true` の場合はランダム、false の場合は `createdAt` 昇順
3. `setSize` 件で切り出す（null の場合は全件）

### 5.2 回答の保存タイミング

回答確定時に即座にReviewStateへ反映・保存する。中断やアプリ終了でも、それまでの回答は保存済みの状態が維持される。

### 5.3 セッションの復元

セット途中でアプリが終了した場合、**セットの再開は行わない**。次回起動時は新しいセットを開始する。

### 5.4 スキップの挙動

「問題不備などで今日は解答したくない」場合の退避機能。**repetition / interval / easiness / dueDate は一切変更しない。**

| 出題範囲 | 挙動 |
|---|---|
| **復習（review）** | `suspendedUntil` に翌日の日付を設定。`dueDate` が到来していても `suspendedUntil` が未来の間は出題対象から除外 |
| **全て（all）** | 出題タイミングに影響を与えない。`suspendedUntil` も設定しない |

いずれも SetItem の `result` は `skipped` として記録し、リザルト画面に表示する。

### 5.5 やり直し（Undo）

- 各問題の回答直前に `prevReviewState` としてReviewStateの完全なコピーを保持する（スナップショットのスタック）
- 「やり直し」実行時、`prevReviewState` をReviewStateにそのまま書き戻す（repetition / interval / easiness / dueDate / lastResult / lastScore / lastAnsweredAt / totalCount / correctCount / partialCount / suspendedUntil をすべて復元）
- `checked` はユーザーの明示的な意思表示のため、**やり直しの対象外**（復元しない）
- スキップの取り消しも `suspendedUntil` の復元により実現する

---

## 6. 技術要件

### 6.1 ストレージ

- IndexedDB に以下のオブジェクトストアを作成
  - `decks`（key: deckId）
  - `cards`（key: cardId / index: deckId）
  - `reviewStates`（key: cardId / index: [deckId, dueDate], [deckId, checked]）
  - `settings`（単一レコード）
- Card と ReviewState の作成・削除・deckId変更は必ず同一トランザクションで行う

### 6.2 データ永続化対策（Safari ITP対応）

Safariでは7日間サイトを訪問しないとIndexedDBが削除される場合がある。長期間の復習間隔が発生するため、以下を必須とする。

1. **`navigator.storage.persist()` を要求**（初回起動時、および未永続時に再要求）
2. **ホーム画面への追加を促す説明をホーム画面上部に常時表示**（未インストール時のみ）。iOS Safari / Android Chrome それぞれの手順を案内
3. **CSVエクスポートによるバックアップを促す**。`lastExportedAt` から既定14日以上経過、または未エクスポートの場合に警告を表示
4. 起動時に `navigator.storage.estimate()` / `persisted()` を確認し、未永続の場合は設定画面に表示

### 6.3 PWA / Service Worker

- `manifest.json` によりインストール可能とする（アイコン、テーマカラー、`display: standalone`）
- Service Worker でアプリシェルをキャッシュし、通信なしで完全に動作させる
- **キャッシュ戦略**：アプリシェルはCache First。キャッシュ名にビルドバージョンを含める（例：`memoapp-v1.0.3`）。`activate` 時に旧キャッシュを削除
- **更新戦略**：新しいService Worker検出時にアプリ内で更新を通知し、ユーザー操作で `skipWaiting()` → `clients.claim()` → リロード。**セット実施中は更新を適用しない**
- プッシュ通知はスコープ外

### 6.4 テーマ（ダークモード）

- ライト／ダークの2配色を持つ。配色値はCSSカスタムプロパティで定義し、個別の指定に色を直書きしない
- 設定値は「端末の設定に従う（既定）／ライト／ダーク」の3値
  - 「端末の設定に従う」は `prefers-color-scheme` に追従し、実行中の切り替えにも反応する
  - ライト／ダークを選んだ場合は端末の設定より優先する
- 設定値は端末ごとの見た目の設定のため、IndexedDB ではなく localStorage（キー `memoapp:theme`）に保存する。
  他端末との同期やCSVエクスポートの対象外とする
- 初回描画前に適用し、ライトからダークへの一瞬のちらつきを起こさない
- `meta[name=theme-color]` を現在の配色に合わせて更新する

### 6.5 表示モード（おとな / こども）

低学年の子どもが一人で使えるよう、暗記を始めることだけに絞った画面を用意する。

- 設定値は「おとな（既定）／こども」の2値。テーマと同じく端末ごとの見た目の設定のため、
  localStorage（キー `memoapp:mode`）に保存し、初回描画前に `<html data-mode>` へ適用する
- **こどもモードのホーム**：デッキの一覧だけを表示し、1件が1つの大きなボタンになる。
  押すとそのデッキの暗記セットが始まる（カード一覧には入らない）。
  カードが0枚のデッキのボタンは押せない
  - 設定・CSVインポート・新規デッキ・デッキ操作・デッキ未所属カード・バックアップ警告・
    インストール案内は表示しない
- **入れない画面**：`#/deck/…` `#/card/…` `#/import` `#/settings` `#/unassigned`。
  ボタンを隠すだけでは履歴やブックマークから入れてしまうため、ルーティングでも
  ホーム（`#/`）へ戻す
- **セット実施中の画面とリザルト画面は変更しない。** ただし、こどもモードでは
  カード一覧・設定へのリンクを出さない
- **おとなモードへの戻し方**：ホーム画面のタイトルを長押しする（既定1.5秒）と確認ダイアログを出す。
  子どもが偶然押して戻してしまうのを避けるため、通常のボタンは置かない
- **文字とタップ領域**：基本の文字を18pxにし、小さい文字（補足・タグ・スコアなど）に下限を設け、
  ボタンの最小高さを引き上げる。配色はテーマ側の値をそのまま使う

---

## 7. CSVフォーマット

### 7.1 基本仕様

- 文字コード：**UTF-8**（BOM有無は両方受け入れる。出力はBOM無し）
- RFC4180準拠。改行・カンマ・ダブルクォートを含む値はダブルクォートで囲む（値内の `"` は `""` にエスケープ）
- **ヘッダ行は必須**。列の並び順は問わず、**ヘッダ名で列を解決する**
- 未知のヘッダ列は無視する

### 7.2 列定義

| 列名 | 区分 | 説明 |
|---|---|---|
| `id` | - | カードID（UUID）。空欄可 |
| `type` | 問題データ | `single` / `choice` / `matching` / `ordering` / `anagram` |
| `question` | 問題データ | 問題文 |
| `choice1`〜`choice6` | 問題データ | 選択肢。matchingは `左:右`（エスケープは2.2） |
| `answerIndex` | 問題データ | 多肢選択の正解番号。**1起点**（内部では0起点に変換） |
| `answer` | 問題データ | 解答文字列 |
| `explanation` | 問題データ | 解説 |
| `checked` | 回答状況 | `true` / `false` |
| `repetition` | 回答状況 | 連続正解回数 |
| `interval` | 回答状況 | 間隔（日） |
| `easiness` | 回答状況 | EF |
| `dueDate` | 回答状況 | `YYYY-MM-DD` |
| `lastResult` | 回答状況 | `correct` / `partial` / `incorrect` / `unanswered` |
| `lastScore` | 回答状況 | 0.0〜1.0 または空 |
| `lastAnsweredAt` | 回答状況 | ISO8601 または空 |
| `totalCount` | 回答状況 | 累計回答回数 |
| `correctCount` | 回答状況 | 累計完答回数 |
| `partialCount` | 回答状況 | 累計部分正解回数 |
| `suspendedUntil` | 回答状況 | `YYYY-MM-DD` または空 |

エクスポートは上記全列を出力する。インポートは `type` / `question` を必須とし、他は欠落時に既定値を適用する。

### 7.3 バリデーション規則

以下に違反する行があればエラーとし、**インポート全体を中止する**。

1. ヘッダ行が存在しない、または必須列（`type` / `question`）が無い
2. `type` が定義された5種のいずれでもない
3. `question` が空
4. choice が7件以上ある
5. 同一CSV内に同じ `id` が2行以上存在する
6. `type = choice` で `answerIndex` が未指定、または1〜（choice件数）の範囲外
7. `type = choice` / `ordering` で choice が2件未満
8. `type = matching` で choice が1件未満、またはエスケープされていないコロンを含まない要素がある
9. `type = single` / `anagram` で `answer` が空
10. `type = anagram` で choice が2件以上ある（0件または1件のみ許可）
11. `id` がUUID形式でない（空欄は可）
12. 数値列・日付列・真偽値列が所定の形式でない
13. `easiness` が `efMin` 未満、`repetition` / `totalCount` / `correctCount` / `partialCount` が負数
14. `correctCount + partialCount > totalCount`
15. `lastScore` が0.0〜1.0の範囲外

### 7.4 インポート時のID解決

| ケース | 挙動 |
|---|---|
| `id` が空欄 | 新規カードとして追加（UUID発行、ReviewStateを初期値で作成） |
| `id` が既存カードと一致 | 4.3の更新オプションに従う。両方OFFならスキップ |
| `id` 指定ありだが既存に無い | 指定idをそのまま採用して新規追加。回答状況列があれば取り込む |

`id` が他デッキの既存カードと一致する場合も「既存カードと一致」として扱い、既存の所属デッキを維持する。

---

## 8. 残課題

1. 検索結果のハイライト表示の要否
2. カード一覧のソート順（作成順 / 次回出題日順 / 正答率順）選択機能の要否
3. 学習統計画面の要否 — 履歴を保持しない設計のため、実装する場合はデータ設計の見直しが必要
4. 解答時間の計測の要否
5. 部分正解が続くカードへの対応 — `partialIntervalFactor = 1.0` では間隔が伸びないため、同一カードが長期にわたり同じ頻度で出続ける可能性がある。運用してみて負担が大きい場合は 1.1〜1.3 程度への引き上げを検討する
